# Patient-Management-System
Patient Management System built with MERN stack featuring JWT authentication, role-based access (Admin/Doctor/Patient), secure password hashing, and full CRUD operations for managing patient records using RESTful APIs.
